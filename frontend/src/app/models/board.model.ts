import { BaseCreateModel, BaseModel, BaseUpdateModel } from './base.model';

export interface Board extends BaseModel {
}

export interface BoardCreate extends BaseCreateModel {
}

export interface BoardUpdate extends BaseUpdateModel {
}
