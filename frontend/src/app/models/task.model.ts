import { BaseCreateModel, BaseModel, BaseUpdateModel } from './base.model';

export interface Task extends BaseModel {
}

export interface TaskCreate extends BaseCreateModel {
}

export interface TaskUpdate extends BaseUpdateModel {
}
